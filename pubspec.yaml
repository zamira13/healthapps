name: untitled
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.1 <4.0.0"  # SDK harus menggunakan format versi range seperti ini

dependencies:
  flutter:
    sdk: flutter

  flutter_image: ^4.1.1
  cupertino_icons: ^1.0.8  # Hilangkan duplikatnya

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Menambahkan assets yang diperlukan
  assets:
    - assets/image/